import type { Metadata } from "next"; import Link from "next/link"; import { PageHero } from "../components/PageHero";
export const metadata:Metadata={title:"Pricing",description:"Group tutoring for $10 per student per session and one-on-one tutoring for $20 per session."};
export default function Pricing(){const cards=[{t:"Group Tutoring",p:"$10",sub:"per student per session",list:["Small-group instruction","Collaborative learning","Guided practice","Affordable academic support"],button:"Book Group Tutoring"},{t:"One-on-One Tutoring",p:"$20",sub:"per session",list:["Personalized instruction","Individual learning plan","Flexible pacing","Focused homework and test support"],button:"Book One-on-One Tutoring",badge:"Most Personalized"}];return <main><PageHero eyebrow="Simple pricing" title="Quality tutoring within reach." copy="Choose the support that fits. Pay per session with no subscriptions or hidden fees."/><section className="section-pad"><div className="container-page mx-auto grid max-w-5xl gap-7 md:grid-cols-2">{cards.map(c=><article className={`card relative ${c.badge?"border-2 border-brand-500":""}`} key={c.t}>{c.badge&&<span className="absolute right-5 top-0 -translate-y-1/2 rounded-full bg-gold-400 px-4 py-2 text-xs font-black">{c.badge}</span>}<h2 className="text-xl font-bold">{c.t}</h2><p className="mt-5 text-5xl font-black text-brand-600">{c.p}</p><p className="text-slate-500">{c.sub}</p><ul className="my-7 space-y-3">{c.list.map(x=><li key={x}>✓ {x}</li>)}</ul><Link className="btn-primary w-full" href={`/contact?type=${encodeURIComponent(c.t)}`}>{c.button}</Link></article>)}</div></section></main>}
